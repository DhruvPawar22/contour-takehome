# Apps Script (bound to the sheet copy)

Placeholder — the rewritten `Code.gs` lands here in Phase 2 (see [PLANNING.md](../PLANNING.md), section
3.5, for the design: an installable onFormSubmit trigger plus a time-based safety net, both idempotent
against the backend).

This file is kept in the repo for reference and history, but the source of truth once written is
whatever is pasted into Extensions → Apps Script on the actual sheet copy — Apps Script doesn't deploy
from git.
